export type moviesProps = {
    movies: {
        image: string,
        title: string,
        age: string,
        tags: string;
        description: string,
        category: string
    }[]

}

export type filteredFilms = {
    filteredFilms: {
        image: string,
        title: string,
        age: string,
        tags: string;
        description: string,
        category: string
    }[]

}